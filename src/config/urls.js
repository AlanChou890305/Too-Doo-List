// URL configuration for different environments
const URL_CONFIG = {
  // Production URLs
  PRODUCTION_CALLBACK_URL:
    "https://your-vercel-domain.vercel.app/auth/callback", // 需要替換為實際的 Vercel 域名

  // Legacy Netlify URL (暫時保留作為備用)
  LEGACY_CALLBACK_URL: "https://to-do-mvp.netlify.app/auth/callback",
};

export default URL_CONFIG;
